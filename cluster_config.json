{
    "__default__" :
    {
        "time" : "12:00:00",
        "mem" : "4g",
        "cpus" : "1",
        "jobname" : "{rule}_{wildcards.sample}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}"
    },

    "fastqc" :
    {
        "time" : "12:00:00",
        "mem"  : "4g",
    },


    "cutadapt" :
    {
        "time" : "12:00:00",
        "mem"  : "4g",
    },
    
 
    "star" :
    {
        "time" : "96:00:00",
        "mem"  : "64g",
        "cpus" :"12",
        "jobname" : "{rule}_{wildcards.sample}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}"
    },
    

    "grep_target_from_bamfile" :
    {
        "time" : "96:00:00",
        "mem"  : "64g",
        "cpus" : "8",
       "jobname" : "{rule}_{wildcards.sample}_{wildcards.target}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}_{wildcards.target}"
 
    },

    "grep_target_from_bamfile_fusion" :
    {
        "time" : "96:00:00",
        "mem"  : "64g",
        "cpus" : "8",
       "jobname" : "{rule}_{wildcards.sample}_{wildcards.target}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}_{wildcards.target}"
    },
    
    "filter_umi" :
    {
       "jobname" : "{rule}_{wildcards.sample}_{wildcards.target}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}_{wildcards.target}"
    },
    

    "filter_umi_fusion" :
    {
       "jobname" : "{rule}_{wildcards.sample}_{wildcards.target}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}_{wildcards.target}"
    },

    "filter_umi_fusion2" :
    {
       "jobname" : "{rule}_{wildcards.sample}_{wildcards.target}",
        "out" : "Output/jobs_files/{rule}_{wildcards.sample}_{wildcards.target}"
    },

}
